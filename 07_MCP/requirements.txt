# Core LangChain (v1.x) — chains, agents, tools
langchain>=1.0.0
langchain-core>=1.0.0

# Model Context Protocol + LangChain/LangGraph integration
mcp>=1.0.0
langchain-mcp-adapters>=0.1.0
langgraph>=0.2.0

# Offline retrieval for the RAG stage of the capstone
sentence-transformers>=2.2.0
numpy>=1.24.0

# Live agent step (optional; only needed to call a real model)
langchain-openai>=0.2.0
openai>=1.3.0
