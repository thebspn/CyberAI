# Core LangChain (v1.x) — used by both notebooks
langchain>=1.0.0
langchain-core>=1.0.0
langchain-text-splitters>=1.0.0

# Offline retrieval for the worked example (langchain_example.ipynb)
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.24.0

# Live LLM step (optional; only needed to call a real model)
langchain-openai>=0.2.0
openai>=1.3.0
